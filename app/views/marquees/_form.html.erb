<%= simple_form_for(@marquee) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :text, label: "Texto" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, :value => "Salvar" %>
  </div>
<% end %>
