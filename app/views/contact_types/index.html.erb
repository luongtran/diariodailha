<h1>Todos os tipos de contatos</h1>

<table class="table striped">
  <tr>
    <th>Nome</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @contact_types.each do |contact_type| %>
  <tr>
    <td><%= contact_type.name %></td>
    <td><%= link_to 'Ver', contact_type %></td>
    <td><%= link_to 'Editar', edit_contact_type_path(contact_type) %></td>
    <td><%= link_to 'Excluir', contact_type, method: :delete, data: { confirm: 'Tem certeza?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Novo tipo de contato', new_contact_type_path %>
