<%= simple_form_for(@photo_list) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :sale %>
    <%= f.association :photo %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
